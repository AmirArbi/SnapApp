<Page
    x:Class="SDKTemplate.Views.ReductionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Center" Text="Taper le pourcentage de réduction:" VerticalAlignment="Center" FontSize="50" Height="79"   Style="{StaticResource BodyTextBlockStyle}" />
        <muxc:NumberBox x:Name="Reduction" HorizontalAlignment="Center" Grid.Row="1"  VerticalAlignment="Center" Width="450" Height="50" FontSize="20" PlaceholderText="Réduction"/>
        <Button x:Name="Continuer" Click="Continuer_Click"  Grid.Row="2" VerticalAlignment="Center" FontSize="15" HorizontalAlignment="Center" Grid.ColumnSpan="2" Width="300" Height="45" Content="Continuer"/>

    </Grid>
</Page>
