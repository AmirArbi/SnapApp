<Page
    x:Class="SDKTemplate.Views.Espace_SNAP.Inscription"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate.Views.Espace_SNAP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    xmlns:data="using:SDKTemplate.Models"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="Student" BorderThickness="2" BorderBrush="{StaticResource ContentDialogBorderThemeBrush}" Grid.Row="1" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.7*"></RowDefinition>
            <RowDefinition Height="6*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Height="100" Margin="120,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock  Text="Date" Style="{StaticResource TitleTextBlockStyle}" FontSize="25" Grid.Column="0"></TextBlock>
            <TextBlock  Text="Montant" Style="{StaticResource TitleTextBlockStyle}" FontSize="25" Grid.Column="1"></TextBlock>
        </Grid>
        <Frame Grid.Row="1" Height="2" Background="#707070"  VerticalAlignment="Bottom" Margin="100,0,50,0"></Frame>


        <GridView x:Name="FilteredListView"
                      Grid.Row="2"
                      Margin="10"
                      ItemsSource="{x:Bind inscriptions}" 
                      IsItemClickEnabled="False" 
                    SelectionMode="None"
                      >
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid x:Name="MaxItemsWrapGrid"
                           MaximumRowsOrColumns="1"
                                       Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:InscriptionModel">

                    <StackPanel x:Name="panel" Height="100" Width="1450" Margin="0,0,0,0"  Orientation="Horizontal" >
                        <Grid Height="100" Width="1400" Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="4*"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                <Image VerticalAlignment="Top" Source="/Images/Dépense.png" Width="55" Margin="30,5,20,10"></Image>
                                <StackPanel >
                                    <TextBlock VerticalAlignment="Top" Style="{ThemeResource BaseTextBlockStyle}" FontSize="25" Margin="0,0,0,5">
                                            <Run Text="{x:Bind Date.Day}"></Run>
                                            <Run Text="-"></Run>
                                            <Run Text="{x:Bind Date.Month}"></Run>
                                            <Run Text="-"></Run>
                                            <Run Text="{x:Bind Date.Year}"></Run>
                                    </TextBlock>
                                    <TextBlock VerticalAlignment="Top" Text="Inscription" Style="{ThemeResource BaseTextBlockStyle}" FontSize="20" Margin="0,0,0,5"></TextBlock>

                                </StackPanel>
                            </StackPanel>
                            <TextBlock Grid.Column="1" VerticalAlignment="Top" Text="{x:Bind AmountGain}" Style="{ThemeResource BaseTextBlockStyle}" FontSize="20" Margin="0,0,0,5"></TextBlock>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>

        </GridView>
        <Grid Margin="10"  HorizontalAlignment="Stretch" Background="#171717" BorderBrush="#707070" BorderThickness="1" Grid.Column="0" >
            <TextBlock Text="Recherche :" Margin="20,0,0,0" VerticalAlignment="Center" Style="{StaticResource HeaderTextBlockStyle}" FontSize="20"></TextBlock>
            <DatePicker DateChanged="Date_DateChanged" x:Name="Date" Margin="20,0,20,0" HorizontalAlignment="Stretch" DayFormat="{}{day.integer} {dayofweek.abbreviated}" ></DatePicker>
            <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="15,0,15,0" Click="Button_Click" Content="Retourner" Style="{StaticResource ButtonRevealStyle}"></Button>
        </Grid>
    </Grid>
</Page>
