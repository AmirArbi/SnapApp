<Page
    x:Class="SDKTemplate.Views.Espace_SNAP.Expenses"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate.Views.Espace_SNAP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="150" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>
        <Canvas x:Name="PrintCanvas" Opacity="0"/>
        <ComboBox x:Name="Utilisateur" ItemsSource="{x:Bind employers}" Grid.Column="1" VerticalAlignment="Center" Margin="80,0,80,0" PlaceholderText="Utilisateur" SelectionChanged="Utilisateur_SelectionChanged" HorizontalAlignment="Stretch"></ComboBox>

        <TextBlock HorizontalAlignment="Left" Text="Nom de l'utilisteur :" TextWrapping="Wrap" VerticalAlignment="Center" UseLayoutRounding="True" FontSize="50"   Style="{StaticResource BodyTextBlockStyle}" Grid.ColumnSpan="2" />

        <muxc:NumberBox  x:Name="Amount" Margin="80,0,80,0" Grid.Column="1" HorizontalAlignment="Stretch" Grid.Row="1" VerticalAlignment="Center" FontSize="20" PlaceholderText="Montant"/>
        <TextBlock HorizontalAlignment="Left" Text="Montant :" TextWrapping="Wrap" VerticalAlignment="Center" UseLayoutRounding="True" FontSize="50"  Style="{StaticResource BodyTextBlockStyle}" Grid.Row="1"/>
        <TextBox x:Name="Note" Grid.Column="1" Margin="80,0,80,0" HorizontalAlignment="Stretch" Grid.Row="2" VerticalAlignment="Center" FontSize="20" PlaceholderText="Note"/>
        <TextBlock HorizontalAlignment="Left" Text="Note :" TextWrapping="Wrap" VerticalAlignment="Center" UseLayoutRounding="True" FontSize="50"  Style="{StaticResource BodyTextBlockStyle}" Grid.Row="2"/>
        <Button x:Name="Continuer" Click="Continuer_Click" Grid.Row="3" VerticalAlignment="Center" FontSize="15" HorizontalAlignment="Center" Grid.ColumnSpan="2" Width="300" Height="45" Content="Continuer"/>
        <Frame BorderBrush="{StaticResource ContentDialogBorderThemeBrush}" Grid.RowSpan="3" Grid.ColumnSpan="2" BorderThickness="2"  Grid.Row="1" Grid.Column="0"  x:Name="Add" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="0,0,5,5" Margin="100,0,100,0"></Frame>
    </Grid>
</Page>
