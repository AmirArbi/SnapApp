 <Page
    x:Class="SDKTemplate.Views.Espace_Prof.Archive"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate.Views.Espace_Prof"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:data="using:SDKTemplate.Models"
    xmlns:viewmodel="using:SDKTemplate.ViewModels"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Loaded="Page_LoadedAsync"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <Image Width="100" Margin="40,20,20,0" Source="/Images/enseignant.png" />
                <StackPanel>
                    <TextBlock Style="{ThemeResource TitleTextBlockStyle}" x:Name="Prof" FontSize="45" Margin="0,40,0,0" ></TextBlock>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="Subjct" Style="{ThemeResource SubtitleTextBlockStyle}"  FontSize="30"></TextBlock>
                        <TextBlock x:Name="Subj" Style="{ThemeResource SubtitleTextBlockStyle}"  FontSize="30"></TextBlock>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="Années scolaire :" Margin="20,10,0,0" FontSize="40" ></TextBlock>
        </StackPanel>
        <Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Style="{ThemeResource AccentButtonStyle}" Height="50" FontSize="30" Margin="20,0,20,0">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Symbol="Back" Margin="5,5,15,0"></SymbolIcon>
                <TextBlock x:Name="ReturnText" FontSize="30" Style="{StaticResource TitleTextBlockStyle}">
                    <Run Text="List des Profs"></Run>
                </TextBlock>
            </StackPanel>
        </Button>
        <Grid BorderBrush="{StaticResource ContentDialogBorderThemeBrush}" BorderThickness="2"  Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4.2*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <GridView x:Name="FilteredListView"
                      Grid.Row="1"
                      Margin="20"  
                      ItemsSource="{x:Bind SchoolYears}"
                      ItemClick="FilteredListView_ItemClick"
                      IsItemClickEnabled="True"            
                >

                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="data:Schoolyear">

                        <StackPanel x:Name="panel" Height="180" Width="390">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="/Images/back-to-school-512.png" Width="40" Margin="30,25,20,10"></Image>
                                <TextBlock Style="{ThemeResource BaseTextBlockStyle}" Margin="0,35,0,5">      
                                    <Run Text="{x:Bind SchoolYear}" FontSize="21"></Run>
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal"  Margin="30,5,0,5">
                                <TextBlock x:Name="Subject"    Style="{ThemeResource BodyTextBlockStyle}" FontSize="21">
                                    <Run Text="Debut : "></Run>
                                    <Run Text="{x:Bind Begining}"></Run>
                                </TextBlock>
                            </StackPanel>


                            <StackPanel Orientation="Horizontal"  Margin="30,5,0,5">
                                <TextBlock x:Name="Formule" Style="{ThemeResource BodyTextBlockStyle}" FontSize="21">
                                     <Run Text="Fin : " ></Run>
                                     <Run Text="{x:Bind Ending}" ></Run>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>

            </GridView>
            <StackPanel Margin="15" HorizontalAlignment="Stretch" Background="#171717" BorderBrush="#707070" BorderThickness="1" Grid.Column="1" >
                <TextBlock Text="Niveau :" Style="{ThemeResource BodyTextBlockStyle}" Margin="20,18,0,12" FontSize="30" HorizontalAlignment="Left"></TextBlock>
                <AutoSuggestBox x:Name="Level" Margin="20,0,20,0"></AutoSuggestBox>
                <TextBlock Text="Nom du group :" Style="{ThemeResource BodyTextBlockStyle}" Margin="20,18,0,12" FontSize="30" HorizontalAlignment="Left"></TextBlock>
                <AutoSuggestBox x:Name="GroupName" Margin="20,0,20,0"></AutoSuggestBox>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
