<Page
    x:Class="SDKTemplate.Views.Espace_Prof.ProfDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate.Views.Espace_Prof"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:data="using:SDKTemplate.Models"
    xmlns:viewmodel="using:SDKTemplate.ViewModels"
    Loaded="Page_LoadedAsync"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
        </Grid.RowDefinitions>
        <muxc:DropDownButton Style="{StaticResource CommandBarFlyoutEllipsisButtonStyle}" AutomationProperties.Name="Email" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="30">
            <muxc:DropDownButton.Content>
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE700;" FontSize="25"/>
            </muxc:DropDownButton.Content>
            <muxc:DropDownButton.Flyout>
                <MenuFlyout Placement="Bottom">
                    <MenuFlyoutItem Text="Archive" Click="MenuFlyoutItem_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xED25;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="Modifier" Click="MenuFlyoutItem_Click_1">
                        <MenuFlyoutItem.Icon>
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70F;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="Info" Click="MenuFlyoutItem_Click_2">
                        <MenuFlyoutItem.Icon>
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE946;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </muxc:DropDownButton.Flyout>
        </muxc:DropDownButton>
        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal" Margin="40,10,0,0">
                <Image Width="80" Margin="0,0,20,0" Source="/Images/enseignant.png" />
                <StackPanel Margin="0,18,0,0">
                    <TextBlock Style="{ThemeResource TitleTextBlockStyle}" x:Name="Prof" FontSize="35"></TextBlock>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="Subjct" Style="{ThemeResource SubtitleTextBlockStyle}"  FontSize="25"></TextBlock>
                        <TextBlock x:Name="Subj" Style="{ThemeResource SubtitleTextBlockStyle}"  FontSize="25"></TextBlock>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </StackPanel>
        <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" VerticalAlignment="Bottom" Text="Groupes :" Margin="20,0,0,5" FontSize="35" ></TextBlock>
        <Button Click="Button_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom" Style="{ThemeResource AccentButtonStyle}" Height="50" FontSize="30" Margin="20,0,20,0">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Symbol="Back" Margin="5,5,15,0"></SymbolIcon>
                <TextBlock x:Name="ReturnText" FontSize="25" Style="{StaticResource TitleTextBlockStyle}">
                    <Run Text="List des Profs"></Run>
                </TextBlock>
            </StackPanel>
        </Button>
        <Grid BorderBrush="{StaticResource ContentDialogBorderThemeBrush}" BorderThickness="2"  Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4.2*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <GridView x:Name="FilteredListView"
                      Grid.Row="1"
                      Margin="20"  
                      ItemsSource="{x:Bind groups}" 
                      ItemClick="FilteredListView_ItemClick"
                      IsItemClickEnabled="True"            
                >

                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="data:ProfGroup">

                        <StackPanel x:Name="panel" Height="180" Width="390" Loaded="panel_Loaded">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="/Images/group.png" Width="40" Margin="30,25,20,10"></Image>
                                <TextBlock Style="{ThemeResource BaseTextBlockStyle}" Margin="0,35,0,5">      
                                    <Run Text="{x:Bind GroupName}" FontSize="21"></Run>
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal"  Margin="30,5,0,5">
                                <TextBlock Text="Niveau : *" x:Name="Subject"    Style="{ThemeResource BodyTextBlockStyle}" FontSize="21"></TextBlock>
                                <TextBlock Text="{x:Bind Level}"  Style="{ThemeResource BodyTextBlockStyle}" FontSize="21"></TextBlock>
                            </StackPanel>


                            <StackPanel Orientation="Horizontal"  Margin="30,5,0,5">
                                <TextBlock Text="Nombre d'élève : *" x:Name="Formule"   Style="{ThemeResource BodyTextBlockStyle}" FontSize="21"></TextBlock>
                                <TextBlock Text="{x:Bind NumberOfStudents}"  Style="{ThemeResource BodyTextBlockStyle}" FontSize="21"></TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>

            </GridView>
            <StackPanel Margin="15" HorizontalAlignment="Stretch"  Background="#171717" BorderBrush="#707070" BorderThickness="1" Grid.Column="1" >
                <TextBlock Text="Recherche :" Margin="20" Style="{StaticResource HeaderTextBlockStyle}" FontSize="20"></TextBlock>
                <AutoSuggestBox x:Name="Level" PlaceholderText="Niveau" Margin="20,0,20,0" TextChanged="AutoSuggestBox_TextChanged"></AutoSuggestBox>
                <AutoSuggestBox x:Name="GroupName" PlaceholderText="Nom de group" Margin="20,30,20,0" TextChanged="AutoSuggestBox_TextChanged"></AutoSuggestBox>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
