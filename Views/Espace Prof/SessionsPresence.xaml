<Page
    x:Class="SDKTemplate.Views.Espace_Prof.SessionsPresence"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate.Views.Espace_Prof"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:data="using:SDKTemplate.Models"
    xmlns:viewmodel="using:SDKTemplate.ViewModels"
    Loaded="Page_LoadedAsync"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4.2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="5.5*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Height="100" Margin="120,30,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock  Text="Nom et Prénom" Style="{StaticResource TitleTextBlockStyle}" FontSize="25" Grid.Column="0"></TextBlock>
            <TextBlock  Text="Presence" Style="{StaticResource TitleTextBlockStyle}" FontSize="25" Grid.Column="1"></TextBlock>
        </Grid>
        <Frame Height="2" Background="#707070"  VerticalAlignment="Bottom" Margin="100,0,50,0"></Frame>
        <GridView x:Name="FilteredListView"
                  Margin="7,5,0,20"
                  Grid.Row="1"
                  ItemsSource="{x:Bind sessions}" 
                  IsItemClickEnabled="False" 
                  Grid.RowSpan="2"
                  SelectionMode="None"
                  >
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid x:Name="MaxItemsWrapGrid"
                           MaximumRowsOrColumns="1"
                                       Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:GroupSession">

                    <StackPanel x:Name="panel" Height="100" Width="1500" Margin="0,0,0,0"  Orientation="Horizontal" >
                        <Grid Height="100" Width="1350" Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3.7*"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                <Image VerticalAlignment="Top" Source="/Images/SessionIcon.png" Width="50" Margin="30,10,20,10"></Image>
                                <StackPanel>
                                    <TextBlock VerticalAlignment="Top" Style="{ThemeResource BaseTextBlockStyle}" FontSize="25" Margin="0,0,0,5">
                                            <Run Text="Séance numero :"></Run>
                                            <Run Text="{x:Bind session_id}"></Run>
                                    </TextBlock>
                                    <TextBlock VerticalAlignment="Top"  Style="{ThemeResource BaseTextBlockStyle}" FontSize="20" Margin="0,0,0,5">
                                            <Run Text="Date  :"></Run>
                                            <Run Text="{x:Bind Date}"></Run>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>
                            <Button Name="{x:Bind session_id}" Click="Button_Click" Grid.Column="1" Content="Suprimer" Style="{StaticResource ButtonRevealStyle}" FontSize="15" Visibility="{x:Bind IsAbsent}" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Grid.Column="1" Text="Suprimé" FontSize="15" Visibility="{x:Bind IsDeleted}" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Grid.Column="2" VerticalAlignment="Top" Text="{x:Bind Presence}"  Style="{ThemeResource BaseTextBlockStyle}" FontSize="20" Margin="0,0,0,5"/>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>

        </GridView>
    </Grid>
</Page>
