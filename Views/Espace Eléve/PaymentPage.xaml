<Page
    x:Class="SDKTemplate.Views.Espace_Eléve.PaymentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate.Views.Espace_Eléve"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="0.8*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Canvas x:Name="PrintCanvas" Opacity="0" />
        <TextBox x:Name="Money" Grid.Column="1" HorizontalAlignment="Stretch" Grid.Row="1" Text="" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="20" InputScope="PersonalFullName" Margin="20" PlaceholderText="Montant"/>

        <TextBlock Grid.Row="1" HorizontalAlignment="Center" Text="Montant :" TextWrapping="Wrap" VerticalAlignment="Center" UseLayoutRounding="True" FontSize="40"  Style="{StaticResource BodyTextBlockStyle}" />
        <Button x:Name="Exist" Grid.Column="1" Margin="-2" CornerRadius="0" Click="Exist_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Style="{ThemeResource AccentButtonStyle}" FontSize="30">
            <SymbolIcon Symbol="Cancel"></SymbolIcon>
        </Button>
        <TextBlock HorizontalAlignment="Center" Text="Mode De Paiement :" TextWrapping="Wrap" VerticalAlignment="Center" UseLayoutRounding="True" FontSize="40" Style="{StaticResource BodyTextBlockStyle}" Grid.Row="2"/>
        <ComboBox x:Name="Payement" PlaceholderText="Mode De Paiement" Margin="20,30,20,0" HorizontalAlignment="Stretch" VerticalAlignment="Center"  Grid.Row="2" Grid.Column="1">
            <x:String>Espece</x:String>
            <x:String>Chèque</x:String>
        </ComboBox>
        <Button x:Name="Continuer" Click="Continuer_Click" Grid.Row="3" VerticalAlignment="Center" FontSize="15" HorizontalAlignment="Stretch" Margin="100,0,110,0" Grid.ColumnSpan="2" Content="Continuer"/>

    </Grid>
</Page>
